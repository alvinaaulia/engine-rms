# Hardened artifact schemas

This is the canonical v2 schema directory. It contains corpus-case, expected-result, actual-result, mismatch, metric, test-execution, run-manifest, verification-status, E2E-trace, and reconstructed-baseline schemas. The underscored `artifact_schemas/` path remains only as a compatibility alias for earlier runners.
